name: Hello World
on: 
    push:
        branches: [ main, develop, release/** ]
    pull_request:
        types: [ opened ]
        branches: [ main  ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Request to main branch
        run: echo "Open Pull Request to main branch"
